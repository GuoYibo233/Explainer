# Project conventions

- Chat with the user in Chinese. Every file in this repo (skill text, templates, domain notes, generated pages, commit messages, comments) is written in English.
- This repo holds the `learn-by-doing` skill. `SKILL.md` at the root is the entry point; `templates/` and `domains/` are its bundled resources.
